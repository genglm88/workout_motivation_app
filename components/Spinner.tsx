import React from "react"
import { BeatLoader } from "react-spinners"

const Spinner = () => {
  //   return <BeatLoader speedMultiplier={1} color="#36d7b7" />
  return <BeatLoader speedMultiplier={1} color="#FFF" />
}

export default Spinner
